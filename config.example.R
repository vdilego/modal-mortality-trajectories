# Copy this file to config.R and add your HMD credentials.
# config.R is git-ignored and should never be committed.
Sys.setenv(HMD_USERNAME = "your_username")
Sys.setenv(HMD_PASSWORD = "your_password")
